<?xml version="1.0" encoding="utf-8"?>
<!-- URL로 링크 추가 > 폴더 선택 Spinner - 열었을 때 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/white" />
                    <stroke android:width="1.3dp" android:color="@color/main_color" />
                    <corners android:radius="10dp" />
                    <padding
                        android:top="9dp"
                        android:left="14dp"
                        android:right="14dp" />
                </shape>
            </item>
            <item
                android:drawable="@drawable/ic_spinner_drop_up"
                android:gravity="top|end"
                android:width="14dp"
                android:height="14dp" />
        </layer-list>
    </item>
</selector>